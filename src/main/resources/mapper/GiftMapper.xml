<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cy.dao.GiftDao">
	<insert id="insertObejct">
		insert into gift
		values (null,#{id},#{giftIntegral},#{giftName},#{giftNumber},#{giftPrice})
	</insert>
</mapper>